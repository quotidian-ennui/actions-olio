repos:
  - repo: local
    hooks:
      - id: shellcheck
        name: shellcheck
        entry: shellcheck
        fail_fast: true
        types: [shell]
        language: system
        stages: [pre-commit]
